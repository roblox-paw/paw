[package]
name = "compiler"
version = "0.0.0"
edition = "2024"
build = "build.rs"

[dependencies]
miette = { version = "7.6.0", features = ["fancy"] }
strum = { version = "0.28.0", features = ["derive"] }
thiserror = { workspace = true }
